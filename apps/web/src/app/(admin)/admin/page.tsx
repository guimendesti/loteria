/** BO-01+ — Dashboard do backoffice (docs/08 §D.2). Placeholder — KPIs/gráficos/funil fora do escopo deste shell. */
export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold text-ink-900">Backoffice</h1>

      <div className="mt-6 rounded-lg border border-dashed border-ink-200 bg-white p-8 text-center text-sm text-ink-400">
        KPIs (usuários, MRR, churn), gráficos e saúde do sistema (BO-01 a
        BO-05) entram aqui — fora do escopo deste shell.
      </div>
    </div>
  )
}
